# PWA Demo App

## 📱 About the App

This is a **Progressive Web App (PWA)** created using **Vite + React** and **vite-plugin-pwa**.  
The app is installable and runs in **standalone (app) mode**, providing an app-like experience using web technologies.

---

## ℹ️ App Information

- **App Name:** PWA Demo
- **Application Type:** Progressive Web Application
- **Framework:** Vite + React
- **PWA Integration:** vite-plugin-pwa
- **Deployment Platform:** Vercel
- **Run Mode:** Web + Installed App (Standalone)

---

## 🖼 App Icons

The application uses the following icons for installation and splash screen:

| Icon File      | Size      |
| -------------- | --------- |
| `icon-192.png` | 192 × 192 |
| `icon-512.png` | 512 × 512 |

**Icons :**

These icons are configured through the PWA manifest generated by the PWA plugin.

---

## 🚀 Features Supported

### ✅ App Installation

- Installable on desktop and mobile devices
- Runs in standalone (app) mode

### ✅ Offline Support

- Works without internet connection
- Managed automatically using service worker

### ✅ Push Notifications

- Permission-based notifications
- Supported on HTTPS environments

### ✅ Hardware Access

- Camera access
- Microphone access
- Location access (with user permission)

### ⚠️ Fingerprint Authentication

- Supported indirectly using **WebAuthn**
- Browser handles biometric verification
- No direct access to fingerprint sensor data

### ⚠️ Device Lock (App-Level)

- **OS-level device lock is NOT supported in PWAs**
- App-level screen lock is implemented instead
- The application locks its interface after a period of inactivity
- User must re-authenticate to continue using the app

> Note: This is an application-level lock, not a system device lock.

---

## 📝 Notes

- PWAs enhance web apps with native-like features
- Some system-level features are restricted due to browser security
- This project demonstrates both supported features and limitations of PWAs
- Designed for educational, demo, and assignment use
- hello
